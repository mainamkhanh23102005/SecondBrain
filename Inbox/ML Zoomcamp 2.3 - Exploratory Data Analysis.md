welcome back this is lesson three of machine learning zoom com session two and we stopped in the previous lesson on looking at the columns of our data frame so we wanted to understand what columns are there and what they actually mean and this is what we&#39;ll do now we&#39;ll do exploratory data analysis we want to understand how the data looks like just to get a feeling what kind of values are there and just to learn more about the data and about the problem so we already know that some of these some of these columns are strings right we also know that some of them are numbers and yeah so we have basically strings and numbers right so let&#39;s try to see um what we actually have there so for that so this is our data frame so what we can do is we can take a look at each each column here and try to understand what kind of data we have there so we have this columns right which gives us the list of columns what we can do is we can just iterate over them and for each of these columns print some statistics and maybe print some values and see what kind of data is there so let&#39;s do this so first we can print the column name like column name and maybe some values so print if so it it tells us okay we have this uh so let me just add one extra line right here so we have model make and this uh is how the first five values look like so then so basically this is the manufacturer of a car model year so this is maybe not very informative because these are the first values so maybe what is more interesting for us is to look at unique values because yeah here we get that it&#39;s all bmw it&#39;s interesting to know what are the other values that we have there so what we can do is let&#39;s say called this method unique and it returns unique values in the in the series so bmw will be only once don&#39;t want to see all unique values let&#39;s just take a look at first five so um and perhaps what will also be interesting is to know how many unique values are there and for that we use this end unique which basically which basically tells us how many unique values are there so for example for the first one we have is manufacturer and so these are the first unique values that we have bmw audi vr mercedes-benz chrysler and there are 48 different values so for model of course model is more granular so there are more different models because for each manufacturer they have multiple models so that&#39;s why we have a lot more values so here is a numerical variable so this is numerical column it has different years 28 of them so this is the type of engine so fuel that it gets like i&#39;m not the car expert i know there&#39;s diesel and there&#39;s others i guess so there are ten different types then this one is engine horsepower it&#39;s like how powerful the engine is how many cylinders are there transmission type driven wheels and all these kind of things we potentially can know about the car so all these characteristics then highway mpg i think it&#39;s highway miles per gallon like how many miles it can drive per gallon of petroleum like a few then city like how many miles you can drive in a city per gallon and popularity this is something the authors of this dataset did is they took a look at twitter and instructed how many mentions uh a car had so basically the idea here is a more popular car has more mentions and then finally this msrp this is what we want to predict so these are the the prices of cars so of course there are many many different values here okay so this is the data we have and now what we will do next is we will look at this price this price column closer so for that we will use we will visualize it we want to look at graphically because looking at these numbers is not very informative so we don&#39;t see the big picture so for this for plotting we will use two libraries so one is called matplotlib multiplied leap and the other one is seabourn so mud plot leap is more like low level one and seabourn is a library on top of it that makes things easier so we&#39;ll just see in a moment how to use this and this line here is needed to make sure that all these plots can be displayed in a notebook so we need to have this so what we want to look now is at the distribution of prices to see how many different uh prices are there and so i&#39;ll just now if you know what histogram is this is what we want to do if you don&#39;t you&#39;ll see now so we want to plot the histogram of msrp and it will just show us the shape of data so this is not very easy to see let&#39;s use smaller amount of bins so bins is basically uh so now let me execute so you see uh here this uh this bar so bins is how many bars we actually have so here we would have 40 bars or 50 sorry so we can also say let&#39;s have around 10 so the bars will be a lot bigger right so let&#39;s use 50. and what we see here is uh so this is how the distribution of data until this notation here so this actually is 1 million so this is scientific notation uh one e6 means ten to six and yeah so this is basically one million so what we see is the there are a lot of prices that are pretty cheap so like most of the cars are here right and then there is very few cars that are super expensive so maybe there is just one car that costs one million maybe there is one car that i said it&#39;s two millions so there is maybe one car that costs two million there is maybe one car that costs 1.5 millions maybe there are a few cars that cost 1 million so there are not so many of them right and the majority like most of the data is concentrated here and this effect is called like this kind of distribution is called long tail distribution because you can think of this as a tail all right so this is like there now it&#39;s very shallow so there are not so many values but they have huge values but most of the data is located here so this is a long tail distribution so we need to zoom out to zoom in a bit so for that what we can do is we can just let&#39;s say look at prices that are not so large let&#39;s say 100 000. so here it&#39;s a bit clearer so now we can actually see and [Music] i think what we are looking at here is this part right so only a little bit here so we don&#39;t look at this at all at this stage so we look only at this here we just zoomed in on that so this is how it looks like so this one is uh it&#39;s easier to actually see visually so most of the cars they are so yeah so this is like maybe the average price somewhere here um and uh yeah so this we see that this is actually one uh 1000 so we have this strange peak of cars that cost one thousand probably this is the minimal price that is possible uh to put on this platform that&#39;s why we have a bunch of cars that cost one thousand and then uh yeah so it slowly goes to uh so i don&#39;t know it&#39;s some 25 000 i think and then like we have a lot of cars around 700 cars that cost around 25 000 and then the number of cars with this price it goes down i think it&#39;s pretty reasonable to expect this kind of distribution so the only the only strange thing is here this 1000 but so while it&#39;s not unexpected to see a distribution like this especially in prices and these long tail distributions are very common for prices because there are most of the things are cheap like for general public let&#39;s say but there are a few super expensive ones there are not so many people who can afford buying expensive ones that&#39;s why there are not so many but there are some people who can actually afford so for those there is this uh like very few ones that is expensive but for the rest of the population we have cheaper cars this kind of distribution is not really good for machine learning so this tail will confuse our model right so it will really screw things so what we want to do is we want to get rid of this long tail and for that what we usually do is we apply the logarithmic distribution so we apply the logarithm to the price and we get let&#39;s say more compact values and the logarithm so let me just show you so it&#39;s for large values the the value of the logarithm is not that large right so you see here we go like from 10 to 1000 but the increase between the logarithm of them is not that high right so it kind of brings very high values and makes them lower so this is one we want to use the problem with just logarithm is if we have a zero here then it complains right so it cannot there is a logarithm of zero doesn&#39;t exist even though it&#39;s not the case for our particular case in our case we have prices that are always 1 000 or more it&#39;s pretty common to add one to all the so we just add one everywhere and this way we make sure that uh we don&#39;t have this situation so the logarithmic one is zero and yeah the logarithm of zero doesn&#39;t exist so we just add one everywhere and that&#39;s why we make sure that you know things things are always positive so and we don&#39;t have these situations when we get problems and then we we don&#39;t want to add this plus one every time time manually so there is actually a function in numpy that can help us it&#39;s a shortcut so it&#39;s called log one uh p so it takes it does uh one piece stands for plus one so it adds one to all the values and then it takes logarithm so this is actually the same as uh we did previously just for a few values type it so we see that okay so we see that the values here that we have they are exactly the same so numpy just does this plus one for us so we want to use this for our prices so let&#39;s see let&#39;s call it lock press locks and we will use this lock 1p function so here yes now if we look at this this series now yeah so it looks different now so all these values became smaller we can draw it now we can plot a histogram let&#39;s do this this is how it looks right now so the the tail is gone so you see that uh yeah so there is no tail like all this uh all these large prices they kind of collapse into this area right and all the prices like all the cars for usual consumers they&#39;re still concentrated around here and this this shape now resembles uh a bit this bell curve shape this is this called normal distribution normal distribution and having a distribution uh that looks like normal even though yeah we have here this weird peak but anyway so this looks more like normal so we have like a clear uh center of the distribution and then it uh when it goes to the left or to the right it kind of goes down right so this situation is ideal for models so models if your target variable looks like that models do quite well when they need to predict this i mean they do a lot better than with long tail distributions and long table distributions usually confuse models so we typically want to get rid of multiple distributions and one way of doing this is exactly what we just saw is applying the logarithm of the price okay so um what we also want to do before we finish this lesson is look at the missing values so we have we have some data and some of these values might be missing i don&#39;t know if we saw here like when we were looking yeah so let&#39;s say number of doors you see that there is this none so not a number or in pandas it usually means that this value doesn&#39;t it&#39;s missing so it wasn&#39;t recorded so let&#39;s see how many of them are there so for that there is a function called is now in pandas so this for every cell it says if this the value in this cell is uh null or not is missing or not it&#39;s not super useful in this form that&#39;s why we do sum so it sums across columns and then for each column it tells us how many missing values are there and we see that there are quite a few missing values so for some cars we don&#39;t know what&#39;s the fuel type what&#39;s the market category how many horsepowers are there how many cylinders are there so this is something we&#39;ll need to keep in mind when we actually do when we train our model okay and that&#39;s it so we look at the different values we try to make sense from the data so we need to take a look at this then next we looked at the distribution of the price and concluded that it has long tail that&#39;s why we removed the effect of long tail with logarithm transformation and then we finally at the end we looked at missing values and we saw that yeah there are missing values and we know that before training the model we will need to do something with them okay and in the next lesson we will uh talk about setting up the validation framework before we train a model we need to make sure that we can validate tomorrow and we will set up this framework in the next lesson