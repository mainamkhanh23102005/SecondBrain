welcome back this is lesson five of machine learning zoom comp session two and now we will talk about linear regression so linear regression is a model that we use for solving regression problems and meaning that we use for predicting numbers so the output of the model is a number and yeah you probably remember from the introduction there is regression there is classification and there is ranking so we are now talking about regression and linear regression is a model for solving this and you probably also remember this formula that we used in the introduction lesson so um here g is the our model then x is the feature matrix y is our target so in this case y is price and this this model g will be linear regression okay and we will come back to this form so here x is a matrix we&#39;ll come back to this form but before we do this let&#39;s take a look at a simplified form so let&#39;s look at the same one but here instead of looking at capital uh capital x which is the entire feature matrix we will look at only one observation so one observation will be so this is a just a car some car and this is its price so we don&#39;t look at all the cars at the same time at all the price at the same time we just look at one at one car so this car so this is uh it&#39;s it can be a row in our feature matrix so we can think of it as a vector it consists of multiple elements so let&#39;s say it&#39;s a n dimensional vector so the first element of this is uh x i 1 then we have x i 2 and so on x i n so we have n elements here so this is our um this is our vector right so what we actually want to have is a function that takes in all these uh features right from exon x i 1 to x i n and then produces something that is close to the price right so this this is what we want to do and yeah regarding this access so these are different characteristics of the cars of cars that we have of this specific car so let&#39;s take a look at what we actually have so in our data set and we now will use training data set because this is what we will use for training the data so remember this this capital x it&#39;s feature matrix training so we don&#39;t look at validation we don&#39;t look at testing we only look at training here and let&#39;s take a look at any so we have quite a few [Music] quite a few rows here let&#39;s take a look at row number 10. so this is rolls royce of model phantom drophead coupe which was manufactured in 2015 so we have all these characteristics so let&#39;s just take engine horsepower we&#39;ll take ct miles per gallon and we take popularity it will take this three just to make it a bit shorter don&#39;t want to take more it will be a bit more difficult to explain and yeah so we have that so for this um for this particular car uh we have four uh engine horsepowers we have 453 [Music] that&#39;s the horsepower that&#39;s the power of our engine then four miles per gallon in the city we have 11 and then popularity is 86 so this is our feature matrix so let&#39;s go back here and let&#39;s write it so this is x i where i is actually 10 so and for this x so our first feature is engine horsepower then it&#39;s uh miles per gallon on in the city and then popularity 86. so now we need to write a function that takes in this g i it takes in this x i and then produce produces a prediction for this car so in code it looks something like this so let&#39;s call it g g and then it take gets in this x i and then it does something do something and then return prediction so let&#39;s say we predict that this car costs 10 000 right so and then so this is our uh x i so then we just our function gives us something right and this is something we will need to implement so of course we we don&#39;t leave it like this this is something we implement so this will be our our linear regression so let&#39;s see how we can actually implement this okay there&#39;s the drone board um yeah so we have this so the and we need to combine uh this variable these values in a way that we have something close to this and for that the formula for applying linear regression is this so this uh gxi is first we have the bias term so this is the bias term this is the the prediction that we will make without knowing anything about the car but we actually know something about the car so we know this uh engine uh capacity so it&#39;s in our case it&#39;s uh uh x i1 right and then for this aging horsepower there is some weight w1 so we don&#39;t just take this feature is this we multiply it by some weight then we have another feature which is uh miles per gallon in the city which is xi2 right and then we have weight for this feature as well it&#39;s w2 and then we also have the same one for uh for the last one for popularity so this is the formula we have four linear regressions let me oops so i want to copy it and move it to the next slide so we see here that we can actually replace that part go away we can replace this part this part we can replace with a sum so then we can write it is so first we have the bias term and then we have a sum um the sum goes from uh so because we already use i here we&#39;ll use j here so it&#39;s from one to three we have three elements here so we have w g times x i g so it&#39;s just a more compact form of writing this so now let&#39;s let&#39;s implement this let&#39;s write this in code this is our g we can just call it maybe linear regression linear regression and then so we have our x i and what we need to have is some w 0 which is our bias term to 0 for now and then we have this w which is a vector so we have the weight for each feature so we have this one this one and this one um so i don&#39;t know let&#39;s put them zeros for now or once and yeah this could be zero so what we do is we need to implement this formula now so i&#39;ll just write it one more time so we have it so we have w0 and then we have a sum from one two three w g x i j because we use uh here uh pandas and we use the python so here it should actually go not from one but from zero to three minus one like in this case two so it&#39;s from zero to uh we can make it generic let&#39;s make it n so and this is the number of features we have right so it goes from zero to one so let&#39;s implement this so for us n will be the length of this feature vector and also the length of this weights vector so it should match of course and then here is our prediction so we start with w0 right and then we have a loop for j in range and so we would do it for every element and we say then prediction should equal to prediction so we basically add to prediction every time and then we need to use wg times x i j and then we return predictions this is our simple linear regression so it uh basically does a sum over uh so for every element of our vector and feature vector we multiply it by corresponding weight and then everything goes to prediction so at the end what we have is this prediction has exactly this sum and yeah we can test it it will not be really correct now did i not execute it let&#39;s use this x-i example to execute this so it gives us some predictions so it doesn&#39;t make much sense for now because these values we just came up with them ourselves so let&#39;s put something else so for this one we can use um 7.17 then put this one 0.01 for this one 0.04 and for this one 0.002 so now we we will talk of course about how do we come with these weights but for now so we have some weights we have our feature vector right and we combine them and we have a prediction so we have our bias term which is 7.17 then what we do next is we multiply our feature engine horsepower by its weight 0.01 then we get the next feature which is uh gallons per mile in the city with its weight and then finally we have popularity which is 86 and then the weight for this popularity and this one so we have uh now this is what our prediction uh prediction is made of so multiple parts and as we see at the end is 12 12.3 right 12.3 so let&#39;s think about this so what does it actually mean so we start with this so this is our bias term and this is what we predict about the car if we don&#39;t know anything about this so let&#39;s say we don&#39;t know anything about the car it&#39;s just an average car so what would be the price we predict for this car for this car we say it&#39;s locally from the price it&#39;s 7.17 but we actually do know something about the car it&#39;s not just the car right it&#39;s uh we know that it has that many um horsepowers and then for each of the horsepowers so let&#39;s say if this car had only one horsepower then the price would increase by 0.01 for let&#39;s say 100 times this we will get one right and then so basically the more horsepower the engine has the more expensive the car becomes right that&#39;s uh i think it&#39;s logical right then for the next feature it&#39;s a miles per gallon in the city so apparently the more the more car consumes in the city the more expensive it is right so for any extra mile uh per gallon it&#39;s the price is affected by this much so let&#39;s say if we have a car that has uh 10 miles per gallon then it would actually be less expensive than this by 0.04 and if we increase it by 11 we get we get more expensive cars so for any extra mile per gallon the car becomes more and more expensive and i think it can make sense because the cars that consume more fuel they are fancier probably that&#39;s why you have this relationship and then finally you have this popularity which is number of mentions in a twitter which has a pretty low weight so it doesn&#39;t seem like it&#39;s affecting the price too much so like for every um extra mention on twitter the car becomes just a little bit more expensive like you really need to mention uh and it&#39;s not just one person it&#39;s like how many people mention this car in general so really a lot of people need to mention this car uh for this to be popular and expensive yeah maybe like tesla i don&#39;t know because it&#39;s popular if people talk about this um in social media and then yeah for them maybe this number is very high and i don&#39;t know and for them it really affects the price but for usual cars it doesn&#39;t okay so this is how we can make sense from this and uh remember actually so this value so this is the value we uh arrive at the end so this is not the final price because we did this uh logarithm of y minus one right so now we need to undo this logarithm and to undo the logarithm we need to do exponent right so we need to do exponent that&#39;s why now let&#39;s do this in uh with numpy there&#39;s a function called logarithm and we just do this here and exponent sorry and yeah so for this car the prediction is uh 20 to 222 000 um dollars right so this is the prediction and uh yeah we forgot to do minus one so this is the prediction and actually there is a shortcut uh here we don&#39;t need to write minus one every time so we just use so like we have a shortcut for logarithmic s1p so i think if we put this here we should be back to yeah to this so there they can undo each other this is the prediction for this car so we implemented a linear regression so we saw how to do this so remember the formula that we have so we implemented this formula on a small uh which vector with of size three and it was just one vector and in the next video we will try to generalize it to more examples so see you soon