welcome to the second lesson of our first session of machinery and zoom comp and we will talk we will compare um machine learning systems with rule based systems in the previous lesson we talked about an example where how we can use machine learning for predicting the price of a car and now in this lesson we will compare um like the old way of doing things without machine learning and with uh using machine learning and we will illustrate this using spam detection example so imagine that you have a system so this is a system for emails right so usually you have gmail or outlook somewhere you have emails and you use this for talking to your colleagues for work related things for chatting with friends and so on so like each row here is an email right and everything works well so you can send emails receive emails and then at some point users of our system start complaining about unsolicited emails so it can be promotions about discounts that people have aren&#39;t subscribed to so this unsolicited promotions or even fraudulent messages that try to um people try to cheat other people and ask them to give some money so and we want to fight this so we want to fight all these males we don&#39;t want our users to receive these emails so we want to develop a spam detection system so we want to take these emails and let&#39;s say have a folder for spam here and send all the emails that are spam to this folder right so this is the system we want to develop and for that we want to to build a classifier that classifies things into spam or not spam right so what we can do is we can just take all the spam messages and try to come up with some [Music] to find some patterns so what makes a spam message spam right and what we can see here is um for example we analyze the data and we see that all mails that come from this particular email promotions at online.com they&#39;re all spam messages or we can see that if a tax review is in the title then it&#39;s also a spam message or tax review and all the messages from this domain online.com they are all spam messages and we can come up with uh rules like okay if sender is promotionsonline.com then it&#39;s spam because we did some analysis and we saw that all males from this particular center they&#39;re all spam right and then we can see if subject contains tax review like we see here and if the domain of the sender is online.com then in all the examples we saw internalized all these messages for spam so we can say that if title contains tax review and center domain is online then it&#39;s fun otherwise it&#39;s a good email right so we have all these three rules right what we can do with these rules now we can write a simple program and then code these rules using for example python so we just sit down and write a few lines of python code where we encode these rules saying okay if sender is this particular sender promotion set online.com then it&#39;s spam if title contains tax interview and the sender the domain of the sender is online.com then it&#39;s pump in all other cases we can see that this is a good email so we we write this code we deploy the system and it works until people start complaining about other different unsolicited messages and um you know for example in this case uh yeah this is a different kind of email that people get that talks about deposit so you want a price if you want to get it you have to pay a small fee ten dollars and transfer this to this account so this is the kind of messages people start complaining about we analyze and we see that if all these spam messages contain deposit all good messages don&#39;t contain deposit so we come with a fee with a rule that says if a body contains a word deposit then we mark this message as spam that works fine for a little while yeah we implement this it works fine for a little wine while until a user uses a work deposit for a genuine person would for a totally legitimate reason right so somebody pedro uh paid the deposit and now he wants to get the deposit again so he uses the word deposit but our system incorrectly classifies this message of spam and puts this into a spam folder right so we do analysis again we do we see okay how we can make sure that this doesn&#39;t happen so how can we separate good emails that contain the word deposit from bad emails that contain the word deposit and we come with other rules and we encode these rules again with python and we do this over and over again and you can imagine that this is a never ending process because spam keeps changing the rules uh they need to to we need to keep them up to date so we constantly need to to change them as well some all the rules don&#39;t work and we write a lot of code we wrote more write more code more and more and more and more and at the end it becomes a nightmare to maintain all this code and it&#39;s very difficult the system just every time we change something things break in different places so this is a situation when we need to think okay is there other tool that we can use to solve these problems and the other tool for this is machine learning so we can actually use machine learning to solve this problem in a different way so the way we do this is we first need to get the data and by that i mean in this particular case all the emails then we need to extract some features from these emails so things that we can use to describe emails all these characteristics of emails then we need to extract these features and then we use this to train the model and then after that we can once we train the model we can use this model for classifying messages into spam and not spam so for getting data what usually email providers do they have this uh spam folder right that people can just uh use to mark email as unwanted spam and then it usually goes to the spam folder so if we have a button like that we can just take all the data that users generated because users tell us hey this message is spam so we can take all the emails we have we can take emails that are spam we can take emails that are not spam and we can use them for training in a model right so we took this so we took the emails we took the the labels spam or not spam now we need to define and calculate features and features can be very simple like um things like is the the title long like what is the length of the title if the title is uh longer than uh 10 then this feature is true if the title is 10 or less then the feature is false right and then we can features like this about the length of the body about the particular sender uh and so on and you if you remember that this uh that these features like about this particular sender or the domain or the word deposit they actually come from the rules we have so most of these features um they are based on the rules we previously had and it&#39;s actually a good idea to start with the rules so first you don&#39;t jump immediately into using machine learning so first you do a rule-based system and then you can use these rules as features for a machine learning system right like we did here and so here we have six features each like this is called the binary feature because we have features that are either true or false so they can only take two values that&#39;s why they&#39;re called binary features we have six of them and we can encode all our emails with this feature so let&#39;s see how we can do this so we have an email right and we want to extract these six features so first we look at the length of the title so the title is longer than 10 10 characters and that&#39;s why the feature is true so here like it&#39;s true but yeah so we encode true as one and false as zero so for us the value of this feature is true so it&#39;s one then the second feature is length of the body so the body is pretty long so this the value of this feature is also true then the next one is about the sender name so the the email so the email is not promotions at online.com so that&#39;s why the value of this feature is zero the next sender is uh not this email that is or hard to read so again the value of this feature is zero then uh we see that the domain of the email is actually test.com so if you see let&#39;s test here uh so that&#39;s why so for this particular feature it&#39;s true so we&#39;ll have one here and the same for the last one so the description contains the word deposit it&#39;s here so that&#39;s why for this particular feature the value is actually should be true yeah and finally we know that this particular email is spam because the user marked this email spam so we know that the value uh so the target variable is one so we take this and we put this as our data set so all the features we extracted plus the target variable and we do this for other email for another email for another email and so on until we take all our emails and extract these features from from them and this is what we have right so we have our data set and we have the target variable right so this is our our data and what we do next is we need to use this data to train a model so we take this and if you remember so we put this into a machine learning algorithm so feature features and target variable go in so we train a model and sometimes we call this process of training is fitting so we hit a model and then the output is a model the model that we can use for predicting the if a message is palm or not so we take this model uh let&#39;s see we have a model here that we&#39;ve trained previously right and now for each of [Music] the messages we have we have some predictions right so for example for this message uh our prediction is we say that the probability that this message is spam is 0.8 meaning that it&#39;s 80 percent likely that this message is right for example for the the second one can be 60 uh maybe this one is not spam so only 10 percent likelihood that this email is spam then maybe this one is 0.01 so very unlikely that this is pump for the next one could be i know 70 percent and maybe 40 right then so we have this uh outputs from the model so the model outputs probabilities right so it thinks that this message looks like spam and i&#39;m sure about that with uh probability so my confidence level let&#39;s say is 80 right so now we actually need to to make a decision should we put this in a spam folder or not right so this is the decision we need to make and we can say if something is uh greater or equals to 0.5 so if something has more than 50 percent chance of being spam then we put this in a spam folder so for this uh for this one say it&#39;s spam because it&#39;s uh more than 50 so this one is also spam this one is actually good because uh yeah it&#39;s less than five uh 0.5 right it&#39;s less than 50 so this one is also good this one looks like spam seventy percent is more than fifty percent and forty percent is a good message right and uh everything that we predicted as spam it will go to the spam folder everything that is good will go to the inbox and this is how we can use our model so now to summarize let&#39;s illustrate the difference between these two approaches between the rule based systems and between uh machine learning so in rule-based systems so we hope we have software so uh our system this is what we this is what we called right so we needed to extracted some rules to extract some rules and write code right so we write some code and then we have some data we have these emails that we want to score so we have data and we have code and then for these emails using this code we have predictions right so this is our outcome so if something is spam or not spam right so we hard code these rules into our software system and our software system makes decisions and we as as we discussed this system is at some point becomes quite difficult to maintain that&#39;s why machine learning algo in machine learning it&#39;s a bit different so in machine learning so in case of usual software we produce outcome right so we hard code the outcome in the code but in case of machine learning outcome is the input to machine learning algorithm so we know that something is spam and we put this to a machine learning so we say if something is spam or not right and then we have the data so this is our emails and for each email we know if something is spammer not so we feed this in a system and then the output is a model and then we can use this model to make predictions for cases when we don&#39;t know the outcome so cases when we only have data then we can use this to uh to make the predictions so we take data we take model and it produces outcome for cases for which we don&#39;t know whether a message is common not we can use this model in our system to sort to sort messages into spam or inbox okay so we talked about a spam classification example and we illustrated the difference between the rule based systems and machine learning and in the next lesson we will talk about supervised learning so all we discussed like examples of price prediction and spam predictions these are examples of supervised machine learning and we formalize this concept a bit more and we talk about different kinds of supervised machine learning like regression classification and ranking