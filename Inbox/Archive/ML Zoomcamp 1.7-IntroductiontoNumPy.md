hello everyone welcome back to lesson seven of machine learning zoom cup session one number one and in this lesson we will talk about numpy so previously you installed you configured the environment you installed all the libraries we needed installed num python numpy jupiter uh pandas and other things and now in this lesson we will do a quick introduction to numpy i&#39;ll show you the functions i use quite often from numpy and things that will be quite useful in that course so let&#39;s start so we have numpy already so now what we need to do is import it so to import a packaging python use import num import and name of the package import numpy but typically what we do is we don&#39;t just import numpy but we use an ids this way it&#39;s shorter to write so instead of writing numpy we can just write np so this is a convention so usually in python list data science this is how you do this so we&#39;ll start with creating arrays and the simplest method for that is called zeros simplest function so it takes one argument this is the size of the array and it just creates an array of this size of the specified size with zeros right can be let&#39;s say 10. there is a similar function to that one called once instead of filling the array with zeros it fills it with once uh then yeah it creates an array of size 10. then if you want to fill an array with some arbitrary number so then you use a function uh called full so first specify the size of the array and then you specify the element you want to fill the array with so in this case it&#39;s 2.5 and array of size 10. so this is how you create arrays with numpy and if you want to convert a let&#39;s say you have a python list so 1 2 3 5 7 12. so for example we have this python list and if you want to create an array from this use a function called array and then pass the list as the argument and then the result is an array so yeah let&#39;s call this array a yeah so we have terry and if we want to access an element of this array then let&#39;s say if we want to access element three which we have here right so what we need to do is we just uh write index two so remember indexing in python starts with zero so and then this is how we access uh the third like in this case element number three and likewise if we want to change this to something else let&#39;s say we want to change it to replace three with 10 so we just use the assignment operator and here after executing this code um here we have 10 instead of three okay um a few other useful functions for creating arrays is one range so this creates a range uh from let&#39;s say if we want to create a range from 0 to 9 we use 10 so 10 is like the last element not in its exclusive so basically up to 10 and by default it&#39;s from 0 to 10 exclusive so in this case it&#39;s from 0 to 9 and let&#39;s say if we want to start with some other number not with three not with zero but with three so we just put another uh parameter here and then it creates a range from uh three to nine this is very similar to a python function called range right so it&#39;s doing the same thing except instead of giving us an iterator from python it gives us a numpy array then another useful function in in numpy is called linspace which creates an array filled with numbers between the first parameter and the second parameter so let&#39;s say we want to create an array with numbers from 0 to 1 so the first element of this array is 0 the last is 1 and then it just creates an array of size 10 of size 11 like specify here and it fills everything between these these two numbers and of course like instead of one we can put anything you you want here and yeah it will just figure out how exactly to to fill it in and so i put here 11 or 10 because if you put 10 then yeah it will not be as nice and if you put 11 it just looks kind of nicer because the steps are rounder and so far we created just uh one dimensional arrays so usual arrays if we want to create i mean numpy it&#39;s possible to create two-dimensional and multi-dimensional arrays so let&#39;s say if we want to create a multi-dimensional array so we use the same function as previously zeros but here we can specify the dimensions of the array so we can say that we want to create an array with five rows and two columns so the first number here is rows takes second is columns and then it creates an array with five columns two rows filled with zeros right in the same way we can create an array from a python list so let&#39;s say we have an array we have a list of lists in python one two three then four five six then seven eight nine so this is a list of lists in python we pass it to the array function and it creates a two-dimensional array for us right so let&#39;s call it n right and again if we want to access elements of this array so we this is basically the same as with one dimensional except we have now two indices and it&#39;s indexed with so it starts with zero so zero one two uh for rows and zero one two four columns and if we want to now let&#39;s say access this element here so element number from row number zero and column number one uh so we use so first we specify zero and then one so it gives us uh this element right and then of course we can can use the assignment operator so if we want to replace this uh 2 with 20 so we just use the assignment operator and then yeah we see that it changes so now we change the this element to 20. if we only want to access uh rows then we instead of passing uh two indices here we can pass one and let&#39;s say if we want to get the zero row we to say pro number zero row number one or row number two right so this is how we access the entire row and again we can use the assignment operator let&#39;s say if we want to rewrite the entire row we can say okay the last row should be just once we can say that so we can assign using python list or numpy array uh like the only condition that it should match the dimension here so let&#39;s do that and now we can see [Music] that the last row changes and then the same we can do with uh if we want to access a column we can get the entire column if we want and for that we need to use only the to specify only the second argument but yeah let&#39;s say if we want to get the second array a second column we use one but we cannot leave it empty so we need to put something and we put the column column operator meaning we want to get all the rows but just keep the the column indexed by number one and we get this we get this second column here right and actually this column operator means return all the rows so we can just use that as it returns everything and yeah so let&#39;s say if we want to get the last column this is what we get and likewise we can assign something else to this let&#39;s say if we want to reassign it to zero one two we do that and then when we look again at um our array we see that we managed to change it here right so this is the same so these are two dimensional arrays so next what we can do is we can generate arrays randomly using this random package in numpy so let&#39;s say if we want to generate a two dimensional array of size with five rows two columns this is what we do so we just specify the number of rows number of columns and it gives us an array with random numbers between zero and one so this is like a uniform distribution standard uniform distribution so and every time we execute this cell we get a different number but if we want to make the results reproducible and if you run this code and if i run this code we get the same numbers it&#39;s possible to do this by fixing the random sheet because these numbers they are not really random they are students pseudorandom so they are generated they are generated and we can set the seed of this generator and if we do this the sequence of random numbers that is produced on my computer and on your computer will be the same so let&#39;s say if we set seat to two then we get an array that looks random so these numbers are random but every time we execute this cell the results are the same right because we fixed the random sheet like the seed number here for the random generation generator that&#39;s why if you execute you probably will have the same one a bit varies depending on the version of numpy or operating system you have but in general the sequence you get the the matrix you get should be the same as i get and then so here we sample from the uniform distribution standard uniform distribution there are many different distributions so let&#39;s say if we want to draw numbers from the standard normal distribution we just add an n here so it&#39;s around around n meaning and normal and then the distribution looks a bit different right and then for integers like let&#39;s say if we want to have numbers between um 0 and 100 so one simple thing we can do is we can multiply every number here by 100 and actually uh since this is numpy arrays we can just use that and this is something i&#39;ll show you explain later in this section but yeah you can just multiply every element by 100 and then you will get instead of having numbers between 0 and 1 you will have numbers between 0 and 100 but if you want there is also a function that you can use for generating random integers so you can specify here the low lowest numbers the highest numbers uh that is possible to get i think it&#39;s this one is includes included this one is probably excluded so it&#39;s between 0 and 99 and then you can say that i want to have five rows and two columns and it will generate you a matrix and a two-dimensional array like that with random integers so yeah here we did this multiplication let me explain how it works so now just create an array first a range of five so it&#39;s numbers from between zero and five um four in this case right and uh we can do let&#39;s say what if we want to add a number to this array so we want to add one to all the elements of this array so what we can do is we can just write like that instead of writing a for loop so if it was a usual python list then we won&#39;t be able to do this we would need to write a loop and then add number to each element but numpy makes it easier for us we can just write plus one one and it adds an element to each number here and let&#39;s say if we want to multiply by 2 then every element of this array you can see so 0 is multiplied by 2 this one is multiplied by 2 so basically every element is multiplied by this number and this is what we used here we just multiplied everything by 100 and of course we can divide it subtract do everything we want and not only that we can also chain them so let&#39;s say we can first divide multiply by 2 and then add 10 and then this is what we&#39;ll have and even let&#39;s say take uh the power of it so yeah so we can change this say operation anyway we want and uh yeah we can do that and uh yeah like whatever we want let&#39;s save it to a different array so now we have array one array a and three b what we can do also we can combine combine these two arrays so we can do element wise sum so we can take let&#39;s see so for for b yeah so what we can do here is uh add elementwise these two arrays so for first element we would compute the sound for second element we would compute the sum and you can see here that uh let&#39;s say the last element here is the sum of these two or the second is some of these two basically does the sum element twice and again we can multiply we can can also divide we can do anything we want here right and then again we can chain it with all these different operations um and then uh yeah so we can do that we can also do compare numbers element-wise so comparison operations are also element-wise operations so let&#39;s say if we want to do element-wise uh comparison with so let&#39;s say we want to see if elements of this array which elements of this array are higher or equal to two and then yeah so let&#39;s print it so we see that the first one is less than two second one is less than two but this one is higher or equal to two because it&#39;s two and then the same is true for um for the rest right so we can do this with elements with just numbers right or we can do it with other arrays so let&#39;s say we can see which elements of array a are greater than the elements of a b and then we see okay like this one is actually smaller the second one is also smaller because zero is more than one uh one is smaller than one point four 44 but this one like for this one actually two is bigger then three is bigger and four is bigger right so that&#39;s why we have false false and then through through so we can do it uh you know for compare elements of multiple arrays and we can also see which elements of a are actually greater than a b if we write something like this and let me execute it and then explain so what happens here is uh so this this statement returns a boolean array like that right so it returns an array with false false through through and when we do this it basically returns all the elements of array a for which this condition is true right so we know for this element this condition is true for this element and for this element and these are elements uh two three and four right and this way we can look at all the elements that uh that satisfy this condition so these were element-wise operators operations and there are different kinds of uh operations that i call summarizing operations because instead of returning another array like here so when we do let&#39;s say when we do this comparison operation we compare a with 2 we get another array but there are some operations that instead of returning and you array return a single number so an example of this is the min operation so mean returns the smallest number in this case it&#39;s zero you can also return the next maximum number maximal which is four you can also compute the sum of all the arrays which is 10 here so it&#39;s like zero plus one plus two plus three plus four is zero or you can compute the average number which is mean right so these are summarizing operators there is also standard deviation if you&#39;re interested and this is how you do this and it of course works also for two-dimensional um so which one is f we have let&#39;s say n right so we have this n so we can also compute the sum of all the elements of m and or find the minimal number and so on so this is a quick introduction there are of course more a lot more useful functions in numpy so i&#39;ll include the link where you can read more about some of the things so there are things like let&#39;s say if you want to find the minimal number in each row of your two-dimensional array what do you do or what if you want to sort and things like this one so for that check the link and in the next lesson we will quickly go through the basics of linear algebra and we will talk about the multiplication operations