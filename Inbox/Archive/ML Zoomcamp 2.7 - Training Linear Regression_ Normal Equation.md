okay welcome back this is lesson seven of machine learning zoom cup session two and we will talk about training the machine learning model so in the previous lesson we saw how to apply a model and we know how to when we have a feature matrix and we have the weights how to make predictions now we need to answer the question how do we actually come with weights and we will do this now so uh remember that for us this g of x the function we have looks like that so this is uh capital x the feature times the future matrix times times the weights vector so this is this is how it looks like and what we want to do is we want this to be close to y right so ideally we want this to be equal to y but often it&#39;s not possible to find the closest solution to this like ideally so we we need to solve this uh system so this is a system we need to solve it for w so we need to find a way to to find w right let&#39;s say this x is invertible so it means that the there exists x minus 1 matrix x to the point power of -1 which is an inverse and we need to multiply it from this this side as well minus 1. so in this case if if this inverse existed then this cancels it becomes i and what we have is this so this is how you would solve this system if inverse existed for x however as we talked previously this x is usually a rectangular matrix right so it has a lot of rows and a few columns perhaps so this has a dimensionality of uh m times uh and minus plus one right and it&#39;s not square definitely not square so for this matrix the inverse doesn&#39;t exist so we cannot just go ahead and make numbers so for this system that we have that we have the solution doesn&#39;t exist we can try to find an approximate solution to this by multiplying this by x transpose here so this matrix is called the gram matrix and for this matrix transpose for this matrix the inverse exists usually um because it&#39;s squared so it&#39;s always uh it&#39;s always squared so i think it&#39;s the dimension of this will be n plus 1 times n plus 1. right so for this one uh inverse should exist it doesn&#39;t always exist but we will talk about this a bit later but it&#39;s squared so inverse can exist and we can try to invert this matrix so let&#39;s do that let&#39;s move it a bit so what we can do now is we can multiply this by the inverse of this graph matrix and multiply it here by the inverse of this graph matrix so here this is inverse of this one it means that this thing goes away it becomes identity matrix which means that we are left with this part so it&#39;s now so did we used to have uh i here and i times w equals w so this is uh this is gone we can just remove it so this actually gives us a way to find w this w is not the solution to the system because the solution doesn&#39;t exist but this w is the closest possible solution to that system i&#39;m not going to prove it now so there are actually proofs for that for this claim and they involve a lot of mathematics and there is a good book about this called elements of statistical learning so you can refer to that book if you&#39;re really interested in the derivation of this how to derive this because what i showed you now is more like on the intuitive level but there is also a bit of theory behind that as well that is quite interested if you if you like this kind of things and go check that i recommend it for that so now we have this formula let me show you uh it one more time now we need to implement this so let&#39;s call this strain linear regression and the input to this is we need to we need the x right and we need the y so the input for this is x and y i will not implement it right now let&#39;s just try to [Music] try to go through this to understand what&#39;s going on here so first we have this x so we can use the same x so this x is a bit problematic because it has more columns than rows i think what we can do is maybe just come up with more examples so [Music] it&#39;s problematic because for this uh for this kind of matrix the solution the inverse will not necessarily exist will probably not exist at all so let&#39;s just come up with a matrix ourselves so i&#39;m doing that i&#39;m creating a list of lists so the first column is uh yeah let&#39;s remove the ones we&#39;ll see how to add them and yeah we need the commas and i&#39;ll just copy this thing one more time one more time and change it to let&#39;s say two five just have like a bit of variation yeah i think this looks okay i hope the inverse for this one will exist okay so we have this matrix right and what we need to do next is uh do so let me just have it here so we know what we are implementing so we have this gram matrix the inverse of this then we have x transpose and then we have y and this is our vector w so we&#39;ll have it here on our screen so first let&#39;s implement the the gram matrix that&#39;s calculated so that it&#39;s pretty simple yeah of course it should be x so we have the grammatics let&#39;s call it a x t x and then we need to find its inverse and we talked about this in the previous lesson how to find the inverse of this matrix and then it gives us some inverse so we can call it x t x if which is inverse and we can quickly check that the actually the dot product between them the multiplication between them gives us yeah it gives us an identity matrix we have ones on the diagonal so it&#39;s not exactly identity matrix but there are these numbers they are super tiny so they are very small they are just uh yeah we can safely ignore them so we can treat this as zero okay so the this is the reason for that is like machine precision is not it&#39;s finite so like it cannot do things with uh infinite precision and that&#39;s why uh yeah there are some leftovers anyways so we have this one right and now we need to multiply it with x transpose and then multiply it with y and we don&#39;t have a y of course so let&#39;s have a y can be i don&#39;t know i&#39;m just coming with this on the spot so how many we have i need more so let me just duplicate this and i think we need one more okay yeah so this is our actually this is our w and we forgot about one thing so we forgot about this bias term right so what we just did what i just did because i forgot to add zeros like sorry ones here that basically we trained the model but we didn&#39;t train the biostrong sometimes it&#39;s possible also to sometimes these models like that make sense but we want to include the bias term because as we talked previously uh this bias term gives us the baseline so this is how much a car should cost if we don&#39;t know anything about this car so this is the baseline and in case we don&#39;t include the bias term then yeah we don&#39;t know like what what should we base our prediction on so let&#39;s add once here so we don&#39;t have ones here and what we need to do is add another column here at the beginning and for that we need to first if you remember there is a function called once and we just need to append this once into at the beginning of this matrix i think it has nine rows so just to be on the sales side let&#39;s just use this shape of x zero which creates which looks at the number of rows and creates this vector of ones and then there is a function in numpy called column stack which takes uh let&#39;s say we can take two vectors and stack them together as calls or yeah we can just take a matrix and so it&#39;s not here because of this scientific indeed you cannot see this because of the scientific notation but basically it took once two times and then added the content of the matrix and the result is a two dimensional matrix so we need just that i know frowning will help yeah it doesn&#39;t help so maybe i&#39;ll do that just to see the results uh okay well at least for this ones we see that because these values are a bit larger that&#39;s why numpy is trying to be smart so basically it adds one one as the first column of this matrix so this is our x now we override it and then we go through this same thing again and let&#39;s call it v4 because it contains all the all the weights so starting from the bias and then the rest and we can decompose it we can say that so v0 is the first one right so this is the biased and uh the the w the rest for usual w take the rest so one and everything after one and this is uh the coefficients that we have for our linear regression so yeah so this is uh this is the bias term and this is w1 w2 and w3 you see that they actually are negative so we didn&#39;t have negative in our example i think because so here maybe if i have larger values here maybe it won&#39;t be negative but basically it means that it&#39;s still negative so negative values here i mean that um instead of adding to the price so we have some price so here it&#39;s not uh i don&#39;t know if we can still call price but basically so in the example we had previously we had positive weights meaning that for extra horsepower that we have in our car the cry with the price increases but here for this particular let&#39;s say if this is uh i don&#39;t know h of a car right for each extra age for each extra year for a car the price decreases so here we have this negative number meaning that the price actually goes down okay and we i think what we do now what we need to do now something we didn&#39;t do is put this in a function so let&#39;s just take this one and just put everything that we did here so first we have once here then we have our x modified x here then we do the normal equation just put them in one line so this is the normal equation right and then at the end we need to return the results and i&#39;ll do this as a tuple so the first element of the tuple will be the bias term and then the rest is yeah the weights and yeah if we do this once again without the ones so we have the y and here we can quickly test it and try and train linear regression so it gives the same results okay so the adding ones happens inside so we as users we don&#39;t need to worry about adding ones to our feature matrix okay so we implemented this function and now let&#39;s use this for our problem let&#39;s use it for predicting the price of car