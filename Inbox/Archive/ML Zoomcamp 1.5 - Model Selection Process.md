welcome back this is our fifth lesson of session number one of machine learning zoom camp and we will talk about the modeling step and we will talk about the process of selecting the best model which is called the model selection process in the previous lesson we talked about uh this modeling step right this is the step where the actual machine learning happens so in this step we try different models so we already have the data extracted on the previous tab so we try different models and our goal is to choose the best model so here you don&#39;t need to worry about logistic regression decision tree neural network mean we will cover them later in other in other sessions but they are different models some of them might work well on this particular problem problem some of them might not work well and what we need to do is try some of them and see what works best and for that we need to have a way of doing this and this is called the model selection process that we will talk about if you think about the way we use a model for our sum example so let&#39;s say now it&#39;s july and we train a model right so we take our uh capital x we take y we produce our model g right and then we deploy the model in august and in august we take the model g that we trained on the data from july and before that and we apply this model to spam messages and it produces some score for example that this email uh the probability that this email is palm is 70 right so this is how we use a model in practice and when evaluating the model we want to sort of mimic this this way of using it so we want to to see how well our model will perform on data it hasn&#39;t seen the model from july didn&#39;t see the data from august it only saw the the data from july right and in july we don&#39;t know how well the model will perform so what we want to do is we want to do something similar of course we cannot go back in the future and take the data from august when we are in july but we can do something that is quite close enough so we can take the entire data set that we have and we can take a small part of this data set let&#39;s say 20 of this and we hide this so we take this data put it away and pretend this data doesn&#39;t exist right so we take the remaining 80 percent right and we use this uh twenty percent for training only right and the twenty percent we don&#39;t use for training so this is sort of the data from august and this is sort of the data from july in a way right so we kind of hide this data and this data set we call it validation data set so this data set is not used in training so let&#39;s say we have this training data set right terrain and we have the validation data set right so what we do is a we need to extract this feature matrix right this capital capital x and we do this from the training data so we have the x we also have the y also coming from the training data and we train our model g only using this x and y right so we have a gene then from the validation data set we extract another x that mosel the model didn&#39;t see it during training and we can call this x x validation x b and again we can do this uh y v right so this is the target variable from the validation data set and the x variable from the validation data set and we have our x over we have our g the model that we trained on the training data set so we take the g and we apply it to the validation data set and we get some predictions now we can call call them just by hard right so these are the predictions what we do next is we need to compare this y hat predictions with the actual values so this is our spam this is not spam and the way it looks in practice is so let me just insert a new page here so the way it looks in practice is uh so so we have this predictions why cut and we have the actual uh target variable right and for um this uh so actual could be one zero one zero one zero so this is palm not spawn spam not spam uh right and for this yeah remember that the output of this thing is a probability so it can be 0.8 0.7 0.6 0.1 0.9 0.6 right this is the probabilities that we later convert to um two predictions so for this one it&#39;s greater than uh 0.5 so for this we predict one sperm for this one we also predict sperm for this one we predict spam for this one we predict not spam this is spam and this is spam again so now we can see so these are our predictions and this is our target variable and we can see in how many cases this is correct so this is correct this is not correct correct correct correct not correct so actually in four hours of six cases it&#39;s correct um so this is uh sixty percent sixty six percent right so in this case the the model we have is 66 accurate so we can do this let&#39;s say for logistic regression we do this and we see that our model is correct let&#39;s say it was our logistic regression so it has 66 accuracy then we take a decision tree which is a different uh model family it&#39;s uh like like the g here and g here are different g&#39;s like different functions and uh for decision tree maybe accuracy is 60 right so then we take another model can be random first right and we see that uh maybe it&#39;s 67 correct and we take a neural network and neural network can be maybe 80 correct for example this i just made up these numbers and we see that uh this model neural network has the best accuracy so we select this model as our best model so this is how it works right however there could be a problem right um i don&#39;t know what happened here like it&#39;s supposed to be like let&#39;s uh like this one think with a heart this is spam and this mail is like a usual name so let&#39;s remove this so there is a problem with this approach so and i want to illustrate this problem with uh like a made-up example but i think it&#39;s a good illustration um let&#39;s say our model is not a logistic regression model it&#39;s not a neural net it&#39;s a coin right so we take uh our twenty percent of data which happen to be these five emails uh and uh um yeah and what we do is now we flip a coin and if a coin uh lands on heads we say it&#39;s palm it has some tails if it lands on tails then we say it&#39;s not spawn right very simple model and we take one euro the first model and uh yeah we make a prediction for our validation data set right so the prediction is so this is actually spam not spam spam not spam not spam so the model says that the first one is spam the second one is spam the third one is not spam not spam and spam so it&#39;s uh yeah so it got only one right right so it&#39;s 20 correct then we take one american dollar and we do the same so it says spam not spam not spam not spam spam right and then it got one two right so 40 correct then we take one polish zloty and uh one polish zloty gives this sequence so g g uh good good uh spam spam uh spam right so one correct uh yeah twenty percent correct then we take one ruble so it can be um good not good good not good right so then it&#39;s uh correct in um yeah it got only one right right and then we take you ukrainian rivena and then uh grievna just produces the exact sequence right it just got lucky right so and then it&#39;s one hundred percent correct and then uh so krivna here got really lucky and we say okay like we see we look at the numbers so like the dollars are quite good um like euros slow test and troubles are very bad at predicting uh spam krivna is the best one it has 100 accuracy for predicting our sum messages but we all know that this is this is really random right so we cannot really control the sequence this uh this coin just got particularly lucky and it managed to produce the same sequence as we had in the validation data set and while this is a two example the exact same thing can happen with a real model so a model can just get lucky so maybe let&#39;s say this is a neural net and this is i don&#39;t know uh decision tree this is logistic regression i don&#39;t know this is random forest and this is uh i know gradient boosting different kind of models and neural net just got lucky at predicting this particular part of the data set so this particular subset of our data it just happen to to do really well on this just by pure chance right and if we take another part if we take another 20 and apply the same model then the results would be totally different so if we take ukrainian grievna and apply to a different 20 probably it&#39;s very unlikely that we again will manage to predict it with 100 accuracy so this in statistics this is called multiple comparison problems when we perform the same comparison many many times so when we use many many different models and evaluate them against the same validation data set one of the models can get particularly lucky for no reason just by chance it gets lucky and it produces a good result this can happen with machine learning a lot right so because at the end these methods are probabilistic so a thing like this can happen that a model just gets lucky to guard against cases like this what we do instead of holding out just one data set we actually hold out two data sets so let&#39;s say this is our data set so we take 20 of this data for validation purposes we take twenty percent more for testing purposes and we take the remaining sixty percent and by the way this sixty twenty twenty this is not set at stone it can be any value and the remaining 60 we take for training right so what we have is we have three not overlapping subsets of our data training data validation data and test data and what we do next is we hide this data set so we just put it away we forget about this and we do the model selection in the same way as i described previously so we take our x we take our y we train our model g then we take our x validation y validation we apply g and we calculate all this accuracy and so on and here at this step we select the best model right so it could be let&#39;s say neural network so we did all this exercise of selecting the best model and then next to make sure that this model didn&#39;t get particularly lucky on this validation set what we do next is we apply this neural network to the test data set so we take the test data set we again extract the our feature matrix or target variable from the test data set and we again do an extra round of validation on the test data set we just want to make sure that um our model the model we think is best is actually the best one and this is how it works okay so let&#39;s say for our example so we had logistic regression we have a decision tree we have random forest and we have a neural net so i think this one was 60 six percent correct this may be 60 i don&#39;t remember i think this was 67 and this was 80. so if we see okay this neural network is the best one right so what we do next is we take the neural network only we forget about this and we take our test data set and now we apply the neural network to the test data set and get a number and we see let&#39;s say the number is it says the accuracy is 79 percent i don&#39;t see okay we have percent here we have 78 here it&#39;s very close it&#39;s reasonably close and we conclude that this model behaves quite well so this process is called the model selection process and this is one of the most important things in machine learning to be able to actually set this up and this is quite simple like as you see all we need to do is we need to split our data set into three parts so let me just so first one is split the data set split the data set so this is our first step right then what you do next is you train a model then you validate the model using the validation data set if you remember we have three data sets so the first one is train then we have validation and we have test data set so we split it into three parts we train our model training data set we apply the model to the validation data set and we see we record what is the accuracy or how good it is and we see that for logistic regression it&#39;s 66 right and we repeat this uh two steps uh three and four two and three how many times we need so we can repeat it for many many different models and then we will have this table like on the previous slide for different models different values right so and then what happens next we did this for many different models and we select the best one the best so we have that and then we using the best value the best model we have from the previous step we applied to the test data and this is our like we check if everything is good and this is the model selection process right so it&#39;s a six step process uh split the data into three three parts do the model selection on the validation data set and then at the end make sure that the model didn&#39;t get particularly lucky uh by testing it one more time um usually uh like here between four and five so let me just create another slide so we have this hundred percent of our data right so we have 20 percent for uh training and we have 20 percent we have 20 for uh validation so train validation test so we train a model here right and we use only sixty percent and um we use sixty percent of training we validate and then we do the testing and here this data set is kinda this part of the data is kind of wasted so we only use it for checking so what we can actually do is instead of just throwing this away completely what we can do is we first we first do the model selection processes usual but then we take we take the training data take the validation data set and combine them into one big train train data then we train a model like model g and then we apply it for testing data so in this case we will not just throw away with the validation data we will use it for training a new model g right which should be a bit better because it uses more data for training and then we check it against the test data set and see okay this model indeed didn&#39;t get just like on the validation set it was actually good and this way we don&#39;t throw away validation uh set we still use it for our final model so this was a bit theoretical uh introduction and in the next section session we actually will use all these things in practice we will code everything but to be able to do this we need to know some tools so we use python for that and in python there are some libraries that we use like numpy pandas and other libraries so they are pretty important so before we move to the practical practical part of applying machine learning we will cover libraries like numpy in the next lesson and we will cover pandas in the lesson after that