welcome back this is lesson six of uh session two of machine learning zoom com and we already talked about a linear regression model and we saw how it works on one simple example and now we&#39;ll try to generalize it to a vector form so how to talk about this and look at all the matrices we have so remember remember we talked about this uh capital x here so how do we go from this notation to this one so let&#39;s talk about this so first of all let&#39;s again write it for a single single vector so remember we have the bias term and then we have a sum that goes from 1 to n then we multiply uh the feature with its weight and this is um this is the formula so this is how we make a prediction for a specific car right and if you look at this you can see that this is nothing else but a dot product so we talked about dot products this is vector vector multiplication and we can actually rewrite it slightly differently using the this vector multiplication notation so this will be vector x e we transpose it and we multiply it with sorry with just the weights with the weights vector so like this annotation is a bit more compact so we can implement this so we already have this linear regression function let&#39;s adjust it so first we will create a function that is doing the dot product and if you remember we already implemented this we called it vector vector multiplication so we start with result which is zero and then for each element we multiply them with each other so x i g times the voltage here it&#39;s of course sum and we return result to this dot product so we don&#39;t need uh here anything anymore so now what we need to do is uh the result will be just plus dot product between um x i and y so yeah using this function our notation uh simplifies quite a bit so our code simplifies quite a bit and yeah so now we implemented this and as we discussed uh actually like this dot like if we use numpy we can just use numpy&#39;s dot okay we can make it even shorter let&#39;s uh look at this so now we have this the this bias term that is kind of hanging out there all alone without without x so it&#39;s kind of we can try to bring it in in this dot product for that we can see we can imagine that there is a that there is one extra feature in uh in our car that is let&#39;s call it i zero i one will call it x i zero and this feature is always one so if it&#39;s one then it means that so let me just move it a little bit this one also so if it&#39;s one it means that it doesn&#39;t really affect it so we can imagine that it doesn&#39;t exist so it&#39;s one always so then our uh vector w becomes uh w0 w1 w2 and so on up to w in so this is a n plus one dimensional vector so we have uh n elements here and then one extra one and then uh this x vector the features it becomes this x i zero x i one x i two and so on x i n and here this uh this is just one right so and when we and when we do dot product between them so when we do this or the other way around we do it doesn&#39;t matter so we multiply w uh zero with one which ends leaves us with zero and then we basically have the dot product between the rest like previously so which is uh equivalent to what we have and it means that what we can do is just use the dot product notation for the entire for the entire linear regression so let&#39;s uh let&#39;s do this so we can call the wnew which will be uh our we don&#39;t need to do it inside so this will be our new uh vector uh with weights and this actually this way like if you do plus on python disk it creates a new list where it just prepends it at the beginning so this is exactly what we need and now this w new contains the bias term and then the rest of the features and then we do the same with x i we just prepared i at the beginning or depend uh 1 at the beginning and now we do a dot product between this and this w and the the result should be the same regression so we see that the result is the same okay so now we have this inductor term in vector form so now let&#39;s go back to thinking about all the examples not just one and in this case we have this matrix which is uh capital x remember and then in this matrix we have uh once always at the beginning so this is because we have uh one here all the time and then we have uh for each row we have the the features so let&#39;s say uh it&#39;s uh the first row so it&#39;s first number one round number two three and four so these are the features of the first row these are the features of the second row and so on then we have for the last one we can even call it uh perhaps n so for the last one it&#39;s or m because we already have n here so it&#39;s m uh and we have x m n so this is uh uh this x for us is uh m by n plus one so there are m rows and n plus one columns right in this and then we have vector w these are on the weights so we have zero and w one and w n right and what we need to do is for each of these rows so for each of these rows we multiply a row with this vector right and then we do the same with this one and we do the same with this one so the result we have is for the first one it&#39;s let&#39;s call it x1 x2 x3 xm so we have x1 dot product with uh w and we have the same for second and then we have the same for the last one right and this is our predictions and we do this if we do this for all the rows all the cards we have in the data set will be our vector y predictions so this is what we predict and you probably can recognize by now that this looks very similar to matrix matrix multiplication in fact it is matrix matrix multiplication so what we need to do to apply linear regression is we take this matrix x we take this vector w and we just multiply them and this is our this our model so what we need to do is just perform matrix matrix matrix vector multiplication and i think we can implement this so just for as an example i&#39;ll take this so let&#39;s say we have more more different observations so we have an observation that remember we need to add once so let&#39;s say this one has 148 horsepowers then 24 miles per gallon and then it has popularity of 1 300. then let&#39;s take another x and another one just make it aligned a bit so this one let&#39;s say it has 132 of horse horses and then this one has 300 something so this is actually the same that let me just copy it here so it&#39;s not very popular oops it&#39;s not very popular but it&#39;s very powerful so this has 25 and let&#39;s say it&#39;s quite popular so we have these observations and now we can put them into into a matrix right so for that we just uh put them so x becomes a list of lists you can take a look at this so it&#39;s a list of lists and if we put this into numpy so now it turns this list of lists into a matrix two-dimensional numbering so we have it and we have our uh w new um vector which i&#39;ll uh let me just go here and that what we need to do now is just do the multiplication between this matrix and this vector okay and here we have predictions so now for each car we have three cars now for each car we have a prediction what is the price for this car and yeah so this is how we can actually implement a linear regression so it will be something like a linear regression and uh yeah so we have all of that and we do vector matrix multiplication right linear regression [Music] and we have the results okay so now we know how to do this for multiple uh rows at the same time so we first generalized it and went from just writing a for loop into recognizing that this is a dot product between a vector with features and a vector with weights then we notice that we can make it even shorter by adding a fictional feature uh one like a virtual feature to our feature vector so our became even shorter and then we generalized it to uh like many examples to a complete feature matrix and we saw that it&#39;s actually nothing else but matrix vector multiplication and you&#39;ve been wondering like where these w&#39;s come from like how do we set the values for them and this is something that we will talk about in the next in the next lesson