okay welcome back this is the third lesson of our first session of machine learning zone comp and we will talk about supervised machine learning so we&#39;ll define more formally what supervised machine learning is and we will talk about a couple of examples like recursion classification and ranking so let&#39;s start so in lesson one we talked about an example of price prediction so we wanted to predict the price of a car and what we did is we kind of supervised the the model we were teachers of a model and we showed the model different examples we showed models different cars and for each car we showed the price and we did it for all the cars we had and we this way we supervised the the algorithms the machines we showed them for this kind of cars this is what the price should be and this is the essence behind this is the idea behind supervised machine learning so in all these cases we have this label and this is how we supervise machine learning we show them we teach the machines by showing them examples and from these examples the machine is able to learn the patterns and use these patterns to generalize to new examples so let&#39;s say when we have a car for which we don&#39;t know a price then the machines using the patterns they extracted they can predict the price of a car right so we teach we teach the algorithms and we also talked about a different example of spam prediction so it was exactly the same so we move the teachers we are showing the machines different uh example of spam and non-spam messages and just by looking at these examples the algorithm was able to pick the patterns and understand uh okay if there is the word deposit maybe this is more likely to be a spam message right so these are the examples we saw so far and this is what we actually need to uh this is how the data looks like so we don&#39;t of course show pictures we don&#39;t just show text not always um so in some cases we do show pictures we do show text but most of the time we need to extract features right we need to extract we need to show and we need to tell the models what exactly we want them to predict we tell the model okay we want to predict if something is palm or not spawn so one means palm zero means not spam but most models don&#39;t care if it&#39;s spam or something else or they care if it&#39;s zero or one right so we have these features we have this target variable and we&#39;ll put this in the model and we can uh make it a bit more formal so for like machine learning actually is a branch of computer science and mathematics of applied mathematics so it uses mathematics statistics and so on to be able to extract these patterns so it can see that let&#39;s say this feature uh is called deposit right and if we see that uh often when we see it that if which is this feature is one then more often than not than not uh yeah in three out of four cases i&#39;m like this is probably what example but in 50 of the cases actually if the word deposit is there then it&#39;s a spam so a model tries to extract patterns like that using concepts from mathematics and statistics so this is what it relies on and we will now define it a bit more formally so this feature matrix so this is called feature matrix feature matrix and we call it usually using capital x right so this is our matrix and matrix is a like two-dimensional array where rows are our observations or objects for which we want to predict something and columns are features right so this is first uh spam is a message this is a second message and we have all this message messages as the role of this matrix and then for each row we have a corresponding like we have a matrix y right so this is uh actually this is a lowercase y so this is um a vector right so this vector e for each row of the matrix we know if this is spamanot so it just contains zeros and ones in this case right so we have this x and y capital x is the matrix with feature a smaller lowercase y is our vector with um with target variable and i know that many of you um like maybe you studied math but uh forgot long time ago so you can think of this as y as one dimensional array so it&#39;s just an array with numbers right so there&#39;s zero one uh zero and so on so this is a one dimensional array right and x is a two-dimensional array so it&#39;s an array of arrays so we have uh basically like here we have six uh six observations so we have six arrays inside and this is a two-dimensional array or a table right so you can think of this as just two-dimensionality right so we have this and now we need to put this in a model right so what we usually do so we have so we have our feature matrix x right and we want to produce why so we want to when we teach a model when we train a model we want to be as close to this y as possible right and we do this by training this function g so g is nothing else but our model it&#39;s our model that takes in that takes in this matrix x and that produces something that is approximately close to the target variable y so this is target variable target and these are our features so again the goal of machine learning is to be able to train to come up with this function g that takes in the feature matrix and produces something that is as close as possible to the target variable right and uh yeah so in our example so target could be the price of a car g um x could be like all the information about cars model make mileage and so on and we want to train our model g in such a way when it takes the x all this information about the car it produces the price that is as close as possible to the price of a uh of a car and remember if you remember the example the actual price of a car was 1.5 000 um and actually the actual price was 1.1 000 and the model predicted 1.5 it&#39;s not always possible to predict the exact price but we want it to be as close as possible right and in this case it&#39;s close enough for our purpose so this is a formal definition of supervised machine learning so this is the goal is to produce this function engine and then the output of this g so we already talked a bit about the output of the price prediction model so the output was a number the price of a car in case of spam classification the output was uh probability right so again this is our x right and this is our predictions so we take our function g which is our model and we put our x the the features and it produces these predictions right so and these predictions they are like let&#39;s say the original target was one for this one it&#39;s palm for this maybe zero zero uh i know one zero one and we try to be as close to the target as possible right so and this is this process of looking at the features and then coming up with this g is called training so we have this function and based on the output it produces and based on the target variable that we have we have different types of supervised machine learning so in the first case and this was the case for the car price prediction example so this is a an example of a regression problem so this is the function g here in this case returns a number that is um like between zero and uh plus infinity so from zero dollars to however many dollars and uh yeah so basically the the the function g here outputs the price uh the prediction for the price right and there are other examples of regression problems that anything that outputs number from can be any number from minus minus infinity to plus infinity or whatever range it is a regression problem so for example if you want to predict the price of uh the price of house and we say okay this is the house with this kind of characteristics like a number of square meters number of rooms uh distance from the center distance from the closest subway station and so on and based on all this information we can say that this house costs for example 1 million or something like this so these are all examples of regression problem and usually the output in this case is always a number then there&#39;s a very different type of supervised machine learning problem type is called classification when we don&#39;t output a number but we output a category so for example if we look at a picture and we say on this picture we have a car so this is a picture of a car then this is a classification problem right we our input is the picture the output is car right so we classify this picture as a picture of a car then the spam example we talked about it&#39;s also classification so we have all these characteristics of an email and then based on this characteristic we predict if something is spammer not so here the function g produces uh like the output is a category and the target file target variable most importantly is a category and then there are different subclasses of classification so there are there is multi-class classification when we want to classify something into multiple different categories so for example let&#39;s say we have images and this could be images of cars this could be images of cats or images of talks and we need to classify images into three different categories this is a multi-class category and of course it can be ten categories it can be a thousand categories it can be as many categories as you need so as long as it&#39;s more than two because for two there is a special subtype of classification problem which is called a binary classification problem and this is the example we already looked at so for sperm classification it was a case of binary classification problem and there we wanted to predict if something is spam or not and if you remember super stomach was 1 or 0 and actually the function g that we had would output a probability between 0 and 1 right and the target variable looked like just an array with ones and zeros so that&#39;s why for binary classification they&#39;re like a special it&#39;s a special subtype of classification problem that is very widely used in practice and then there is a different type of uh different type of supervised learning which is called ranking we will not talk a lot about this course but i just want to mention it exists so ranking is usually used in cases when you want to run something so it can be a recommender system so for example i&#39;m a user i go to an e-commerce website and there are many many products that i am potentially interested in how to select the most interesting one so what happens under the hood is there a function that ranks the items in such a way so the the function tries to understand for me so in this case uh uh like for me as a user what would be the let&#39;s say the probability that i like this item and it looks at all the items that are there and tries to score them give them a score can be just for simply still take let&#39;s think let&#39;s take a score from zero to one so this core everything that is there you assign it a score from zero to one then it takes all these items and returns the top six so here we have the top six right so these are the items that had the uh the biggest score so for example let&#39;s say this can be 0.7 this can be 0.66 uh i have no idea what is that actually but apparently this is something i potentially would like then you know i don&#39;t know a case for uh headphones and things like this so they basically score everything and then they sort everything by the score and then they show me show me top six results so this is how a recommended system works and this is an example of a ranking problem right so we want to have ranked list of items that i will probably want to buy right and if you think about this google is doing something similar so that if i go to google and drive machine learning zoomcamp it wants to show not only what is relevant but also what is most interesting for me so they look at all the documents in the world that contain this particular uh phrase right and then they score them uh they want to see how likely that this document is relevant for me so they again maybe have for this documented probability of relevance is uh 0.9 then for this one maybe 0.85 and so on so they basically show uh there they rank by this relevant score and then they showed the most relevant as the first one so this is an example of uh ranking again and for e-commerce like for e-commerce shops and marketplaces like colleagues this search problem is also quite important so we if you want let&#39;s say to buy an iphone you go to olix and you uh type iphone in search so you want to we want to show the user what is most relevant for this particular user so this is again an example of a ranking problem and yeah in summary so supervised machine learning is about teaching computer teaching an algorithm by showing different examples and the examples i usually we put them in matrix x and then we have the vector y so in matrix x we have we call it the feature matrix these are all vector characteristics of an item for which we want to make a prediction and y is the prediction we want to make right so this is what we want to learn from and then the goal of supervised learning is to come up with a function g right and in such a way that when we apply this g to our feature matrix we get something very close to our target variable right and here inside g and we will talk about throughout the course what this g looks like exactly so here this g actually tries to extract patterns from this matrix x in such a way when we apply this matrix x to g we get something that is very close to target variable so this is the essence of supervised machine learning and then based on the type of target variable we can have regression classification which can be multi-class classification or binary and we can have ranking right and in this course we will focus mostly on classification and we will have a chapter also it will be the next lesson we will talk about regression and actually the case of binary classification this is probably the most widely used type of machine learning i would say of supervised machine learning and you definitely need to you will definitely have a problem that can be solved um as a binary classification problem when you work with machine learning so that&#39;s it for this lesson so in the next lesson we will look at the bigger picture of organizing machine learning projects and we will talk about a special methodology for organizing machine learning projects called crisp them